# parameter-copy-sda
Parameter Copy Tool is a Revit utility that safely copies only common parameters from a reference element to multiple target elements—across families and categories—while preventing invalid or read-only assignments.
